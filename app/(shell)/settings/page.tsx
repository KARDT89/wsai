import { PlaceholderSurface } from "@/components/shell/placeholder-surface"

export default function SettingsPage() {
  return (
    <PlaceholderSurface
      title="Settings"
      eyebrow="Phase 1"
      description="Account preferences, signature, default calendar, approval strictness, and notification settings will be configured here."
    />
  )
}
